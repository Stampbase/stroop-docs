version: 2
# Dependency update pull requests are never auto-merged; each one goes through
# CI and review like any other change.
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "type:infra"
